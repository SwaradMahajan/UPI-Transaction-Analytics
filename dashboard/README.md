
  # Create app as prompted

  This is a code bundle for Create app as prompted. The original project is available at https://www.figma.com/design/htHLXoGHKkoZ0Zm4R8XxYH/Create-app-as-prompted.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  